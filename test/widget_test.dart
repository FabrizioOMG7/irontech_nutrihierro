import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Dummy test for widget test', (WidgetTester tester) async {
    // Isar is problematic in the current sandbox without actual binaries.
    expect(true, isTrue);
  });
}
